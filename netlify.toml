[build]
  publish = "dist"
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"  # ← Recomendado para versiones recientes

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200